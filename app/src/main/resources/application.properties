spring.application.name=back-end

# spring.datasource.url=jdbc:mysql://<mysql_host>/cms?usessl=false
# spring.datasource.username=root

# spring.datasource.password=<mysql_password>

# === PostgreSQL Configuration ===
spring.datasource.url=jdbc:mysql://localhost:3307/cms
spring.datasource.username=Kaleb9301
spring.datasource.password=goDjango@2015#

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# spring.data.mongodb.uri=mongodb://root:<mongodb_password>@<mongodb_host>:27017/prescriptions?authSource=admin"

# === MongoDB Configuration ===
# Use an environment variable when available so you can point the app to Atlas, Docker, or a local mongod
# Default stays localhost for developer convenience
# Disable Spring Boot's Mongo auto-configuration by default. If you want Mongo enabled,
# set `app.mongodb.enabled=true` and provide `MONGODB_URI` (or keep the default URI).
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
app.mongodb.enabled=false
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/prescriptions}

management.endpoint.health.show-details=always
management.health.db.enabled=true


api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M



spring.web.resources.static-locations=classpath:/static/

# -------------------------
# Thymeleaf Configuration
# -------------------------
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false    
spring.thymeleaf.encoding=UTF-8